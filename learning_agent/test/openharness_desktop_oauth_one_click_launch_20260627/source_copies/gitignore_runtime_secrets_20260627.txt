# 忽略 Python 自动生成的缓存目录；没有这行会把本机运行后的 __pycache__ 目录提交进仓库，后续 diff 会被无关缓存刷屏。
__pycache__/
# 忽略 Python 字节码文件；没有这行会把不同 Python 版本生成的 .pyc 文件提交进仓库，其他电脑可能反复产生无意义变化。
*.pyc
# 忽略本项目本地 worktree 容器目录；没有这行会有误把隔离工作区内容递归提交到主仓库的风险。
.worktrees
# 忽略本项目本地 worktree 容器目录内的所有内容；没有这行会有误把隔离工作区文件提交到主仓库的风险。
.worktrees/
# 忽略备用 worktree 容器目录；没有这行会有误把 worktrees 目录里的隔离副本提交到主仓库的风险。
worktrees
# 忽略备用 worktree 容器目录内的所有内容；没有这行会有误把备用隔离副本文件提交到主仓库的风险。
worktrees/
# 忽略真实终端验收运行产物；没有这行会把截图、日志和每次运行结果都提交进仓库，仓库会快速膨胀。
learning_agent/acceptance_controller/runs/
# 忽略未来事件流 session 运行记录；没有这行会把每次 agent 对话 transcript 都提交进仓库，容易泄露用户输入和工具结果。
learning_agent/memory/sessions/
# 忽略临时调试日志目录；没有这行会把本机排查日志提交进仓库，后续 review 会混入大量无关内容。
learning_agent/debug_logs/
# 忽略浏览器截图和下载产物目录；没有这行会把真实浏览器运行时截图、网页产物提交进仓库，仓库体积和隐私风险都会变大。
learning_agent/browser_artifacts/
# 忽略真实 Chrome 本地 profile 配置；没有这行会把用户本机浏览器配置路径提交进仓库，可能暴露隐私路径。
learning_agent/browser_profiles.json
# 忽略真实 Chrome 审计日志文件；没有这行会把真实浏览器连接和操作审计提交进仓库，可能暴露用户浏览器行为。
learning_agent/browser_artifacts/real_chrome_audit.jsonl
# 新增代码+项目瘦身：忽略 Windows Computer Use 截图证据目录；如果没有这行，真实桌面截图和 BMP 证据会继续进入 Git 状态，让项目体积快速变大。
learning_agent/memory/computer_use/evidence/
# 新增代码+项目瘦身：忽略 harness 运行事件目录；如果没有这行，长期任务每次运行产生的 JSONL 事件会持续堆进项目，后续排查和提交都会变慢。
learning_agent/memory/harness/events/
# 新增代码+项目瘦身：忽略根目录 memory 的会话记录；如果没有这行，外层运行记录也会继续留下 transcript 和事件流，项目会再次膨胀。
memory/sessions/
# 新增代码+项目瘦身：忽略根目录 memory 的状态事件；如果没有这行，状态 JSONL 会随着每次运行增长，仓库会被运行历史拖大。
memory/status/
# 新增代码+RuntimeSecretsIgnore：忽略根目录调试日志；如果没有这行，真实 GUI 验收生成的 agent_debug 日志会进入 Git 状态，可能暴露本机运行内容。
debug_logs/
# 新增代码+RuntimeSecretsIgnore：忽略 GUI bridge 运行状态；如果没有这行，真实 GUI 会话 state.json 会混进提交范围，让代码提交夹带本机对话状态。
memory/gui_bridge/
# 新增代码+RuntimeSecretsIgnore：忽略 provider 设置和本地密钥缓存；如果没有这行，secrets.os.json 这类敏感文件会出现在 Git 状态里，存在误提交风险。
memory/gui_provider_settings/
# 新增代码+RuntimeSecretsIgnore：忽略根目录 harness 运行事件和快照；如果没有这行，真实 agent harness 每次运行的 JSONL/JSON 轨迹会污染提交。
memory/harness/
# 新增代码+RuntimeSecretsIgnore：忽略根目录 runtime 执行轨迹；如果没有这行，commands.json 和 events.jsonl 会把本地执行历史带进仓库。
memory/runtime/
# 新增代码+GitHub上传瘦身：忽略 learning_agent 内部状态事件目录；如果没有这行，真实运行产生的 events.jsonl 会被提交到 GitHub，既膨胀仓库也暴露本机运行痕迹。
learning_agent/memory/status/
# 新增代码+GitHub上传瘦身：忽略 learning_agent 的 Computer Use 运行记忆目录；如果没有这行，桌面截图索引、审计 JSONL 和绘图运行记录会混进源码仓库。
learning_agent/memory/computer_use/
# 新增代码+GitHub上传瘦身：忽略根目录 Computer Use 运行记忆目录；如果没有这行，本地 native_dependency_report 等运行状态会被提交到 GitHub。
memory/computer_use/
# 新增代码+GitHub上传瘦身：忽略 acceptance_controller 下误嵌套出来的 learning_agent 运行副本；如果没有这行，错误路径里的终端截图和事件日志会混进仓库。
learning_agent/acceptance_controller/learning_agent/
# 新增代码+GitHub上传瘦身：忽略学习目录里的视频文件；如果没有这行，mp4 教学录屏会让仓库变大，且不属于源码上传的核心。
learning_agent/test/**/*.mp4
# 新增代码+项目瘦身：忽略学习备份中的验收截图目录；如果没有这行，learning_agent/test 里会继续复制可见终端截图，学习目录会越来越大。
learning_agent/test/**/acceptance_run/
# 新增代码+项目瘦身：忽略学习备份中的可见终端截图目录；如果没有这行，visible terminal 截图会被保存进学习目录，项目体积会反复膨胀。
learning_agent/test/**/visible_terminal_run/
# 新增代码+项目瘦身：忽略学习备份中的 PNG 图片；如果没有这行，截图类学习附件会进入 Git 状态，源码仓库会被图片文件拖大。
learning_agent/test/**/*.png
# 新增代码+项目瘦身：忽略学习备份中的 BMP 图片；如果没有这行，未压缩 BMP 截图可能单个就占数 MB，让 agent 项目显得异常臃肿。
learning_agent/test/**/*.bmp
# 新增代码+项目瘦身：忽略学习备份中的 JPG/JPEG 图片；如果没有这行，浏览器或桌面验收图片会继续污染学习备份目录。
learning_agent/test/**/*.jpg
# 新增代码+项目瘦身：忽略学习备份中的 JPEG 图片；如果没有这行，不同后缀的同类截图仍可能漏网进入仓库。
learning_agent/test/**/*.jpeg
# 新增代码+项目瘦身：忽略学习备份中的 WebP 图片；如果没有这行，浏览器产出的 WebP 证据也会继续增加项目体积。
learning_agent/test/**/*.webp
# 新增代码+项目瘦身：忽略学习备份中的 JSONL 事件流；如果没有这行，学习目录会复制大量逐行日志，后续阅读和提交都会变重。
learning_agent/test/**/*.jsonl
# 新增代码+GitHub上传瘦身：忽略学习备份目录里嵌套出来的 acceptance_controller/runs；如果没有这一行，历史验收运行目录可能通过学习副本路径绕过上面的忽略规则进入仓库。
learning_agent/test/**/acceptance_controller/runs/
# 新增代码+GitHub上传瘦身：忽略 CodeGraph 本地索引目录；如果没有这一行，`.codegraph/.gitignore` 等本机索引辅助文件可能被误提交到 GitHub，恢复源码并不需要这些本地索引文件。
.codegraph/
# 新增代码+GitHub上传瘦身：忽略 pytest 本地缓存目录；如果没有这一行，`.pytest_cache` 这类测试缓存可能进入备份仓库，既不能帮助恢复代码，也会制造无意义 diff。
.pytest_cache/
# 新增代码+GitHub上传瘦身：忽略 computer_use_mcp_v2 的运行记忆目录；如果没有这一行，真实桌面运行产生的记忆、日志或证据可能被上传，既膨胀仓库也有隐私风险。
learning_agent/computer_use_mcp_v2/memory/
# 新增代码+GitHub上传瘦身：忽略压缩上下文产物目录；如果没有这一行，长任务运行时生成的大量 compact artifacts 会被误当成源码上传。
learning_agent/memory/compact_artifacts/
# 新增代码+GitHub上传瘦身：忽略 harness 运行快照目录；如果没有这一行，每次 agent 执行产生的 runtime JSON 会进入 GitHub，恢复代码并不需要这些本机运行痕迹。
learning_agent/memory/harness/runs/
# 新增代码+GitHub上传瘦身：忽略 learning_agent 的 runtime 运行日志；如果没有这一行，commands.json 和 events.jsonl 这类本机执行轨迹会被上传，恢复源码不需要这些运行记录。
learning_agent/memory/runtime/
